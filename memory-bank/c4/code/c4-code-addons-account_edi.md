# C4 Code Level: addons/account_edi

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: addons/account_edi
- **Content Hash**: addons-account_edi-root-py-20260701

## Overview

- **Name**: Electronic Data Interchange (EDI) Base Framework
- **Description**: Base module providing import/export infrastructure for invoices in multiple EDI formats (UBL, Factur-X, CII, PEPPOL); handles document routing, format selection, and transmission workflows
- **Location**: addons/account_edi — 2 files, ~20 lines (root); 8 model files + 1 wizard in submodules
- **Language**: Python (Odoo ORM models and wizards)
- **Purpose**: Establishes the EDI abstraction layer for electronic invoice exchange; defines core models (`account.edi.document`, `account.edi.format`) and integration points extended by country-specific and format-specific modules

## Code Elements

### Modules (Root Level)

- **`__init__.py`** — Root package initialization
  - **Location**: `__init__.py:1-4`
  - **Content**: Imports `models` and `wizard` submodules to register EDI data models and interactive wizards
  - **Visibility**: public (module root)

- **`__manifest__.py`** — Module metadata and dependency declaration
  - **Location**: `__manifest__.py:1-25`
  - **Content**: Declares "Import/Export Invoices From XML/PDF", category "Accounting/Accounting", version 1.0, with data fixtures for security (ACL), document views, journal configuration, and cron tasks
  - **Visibility**: public (plugin manifest)

### Key Manifest Fields

- `'name'`: "Import/Export Invoices From XML/PDF" — Feature description
- `'description'`: "Electronic Data Interchange. EDI is the electronic interchange of business information using a standardized format. This is the base module for import and export of invoices in various EDI formats..."
- `'depends'`: `['account']` — Extends account module for document management
- `'data'`: 
  - `'security/ir.model.access.csv'` — EDI document access control rules
  - `'views/account_edi_document_views.xml'` — UI for EDI document list, form, kanban
  - `'views/account_move_views.xml'` — Invoice view extensions (EDI status, format selection)
  - `'views/account_journal_views.xml'` — Journal form extensions (EDI format chooser)
  - `'data/cron.xml'` — Background job for EDI document processing (polling/retries)
- `'category'`: "Accounting/Accounting"
- `'version'`: "1.0"
- `'license'`: "LGPL-3"

## Dependencies

### Internal (Submodules — not walked at root level)

- `models/` — Odoo model extensions
  - `account_edi_document.py` — Core model defining EDI documents, formats, state transitions, and retrieval/sending workflows
  - `account_edi_format.py` — Abstract format registry; defines format capabilities, validators, generators, and parsers
  - `account_move.py` — Extends account.move (invoices) with EDI-related fields and actions (e.g., "Send EDI")
  - `account_journal.py` — Extends account.journal with EDI format selection
  - `account_move_send.py` — Extends account.move.send wizard to support EDI transmission
  - `ir_attachment.py` — Extends ir.attachment for EDI document metadata
  - `ir_actions_report.py` — Extends ir.actions.report for PDF/XML generation hooks

- `wizard/` — Interactive workflows
  - `account_resequence.py` — Re-sequence invoice numbers (may involve EDI document state)

### External

- **Odoo 18.0 Core**
  - `account` (declared) — GL, invoices (account.move), journals, attachments
  - `base` (implicit) — Company, partner, attachment models
  - `web` (implicit) — Views, kanban, forms

## Relationships

The account_edi module serves as a **framework/base** for format-specific modules:

```
account_edi (base framework)
    │
    ├── account_edi_ubl_cii (UBL 2.0, 2.1, CII/Factur-X, PEPPOL BIS 3.0, XRechnung, etc.)
    │
    ├── l10n_XX_edi_* (country-specific formats)
    │   └── Examples: l10n_be_edi_e_fff, l10n_nl_edi_nlcius, l10n_de_edi_xrechnung, etc.
    │
    └── Other format plugins
```

The base module defines interfaces and lifecycle; child modules implement format generation/parsing.

## Notes for Higher-Level Synthesis

- **Framework/Platform** — Serves as the extensibility layer for all EDI functionality; not domain-bearing itself but provides the contract that all format modules implement
- **Account domain** — Lives in accounting but abstracts invoice transmission; logically part of "Accounting & Finance" component
- **Boundary code** — Defines model-to-message and message-to-model translation; integration point for external EDI networks (e-SSENTIALS, PEPPOL, government portals)
- **Extension point** — Intended for subclassing; minimal business logic at root level
- **Cron-driven** — Includes background polling/retry logic (cron.xml); asynchronous document lifecycle management
- **Belongs with** — All EDI/e-invoicing modules (account_edi_ubl_cii, l10n_*_edi_*) synthesize into an "Electronic Invoicing" or "EDI" component
