# C4 Code Level: auth_signup

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: addons/auth_signup
- **Content Hash**: 4f8e3c2d1a5b9e7c6f3a8b1d5e2c9f4a

## Overview

- **Name**: User Self-Registration & Password Reset
- **Description**: Enables user sign-up, password reset, and invitation workflows with token-based authentication and email confirmation
- **Location**: addons/auth_signup — 13 files, ~800 lines
- **Language**: Python (Odoo ORM)
- **Purpose**: Provides self-service user account creation, password reset, and partner-level signup token management integrated with email notifications

## Code Elements

### Functions / Methods

- `ResUsers.signup(values, token=None) → tuple`
  - **Description**: Creates or updates user account via signup flow; handles both new external users and invited partner users
  - **Location**: `models/res_users.py:55`
  - **Visibility**: public
  - **Dependencies**: res.partner._signup_retrieve_partner, ir.config_parameter, mail.template

- `ResUsers.reset_password(login) → action`
  - **Description**: Initiates password reset for a user by login or email; sends reset link via email
  - **Location**: `models/res_users.py:157`
  - **Visibility**: public
  - **Dependencies**: ir.mail_server, res.users.action_reset_password

- `ResUsers._signup_create_user(values) → res.users`
  - **Description**: Creates a new user from template user with signup validation
  - **Location**: `models/res_users.py:109`
  - **Visibility**: internal
  - **Dependencies**: ir.config_parameter, res.users._create_user_from_template

- `ResUsers._get_signup_invitation_scope() → str`
  - **Description**: Returns signup scope configuration ('b2c' for public signup, 'b2b' for invite-only)
  - **Location**: `models/res_users.py:105`
  - **Visibility**: internal
  - **Dependencies**: ir.config_parameter

- `ResUsers.authenticate(db, credential, user_agent_env) → dict`
  - **Description**: Overrides base authentication to handle new device alerts for signed-up users
  - **Location**: `models/res_users.py:119`
  - **Visibility**: public
  - **Dependencies**: ir.mail_server.MailDeliveryException

- `ResUsers._notify_inviter()`
  - **Description**: Sends notification to user who created the inviting partner
  - **Location**: `models/res_users.py:130`
  - **Visibility**: internal
  - **Dependencies**: res.users.create_uid, bus.Bus

- `ResPartner.signup_prepare(signup_type="signup") → bool`
  - **Description**: Prepares partner for signup by setting signup_type token marker
  - **Location**: `models/res_partner.py:113`
  - **Visibility**: public
  - **Dependencies**: None

- `ResPartner._signup_retrieve_partner(token, check_validity=False, raise_exception=False) → res.partner | False`
  - **Description**: Retrieves partner record by signup token with optional validity check
  - **Location**: `models/res_partner.py:121`
  - **Visibility**: public
  - **Dependencies**: res.partner._get_partner_from_token

- `ResPartner._signup_retrieve_info(token) → dict`
  - **Description**: Extracts user info (name, login, email) from a valid signup token
  - **Location**: `models/res_partner.py:134`
  - **Visibility**: public
  - **Dependencies**: res.partner._get_partner_from_token

- `ResPartner._get_signup_url(...) → str`
  - **Description**: Generates a signup or password reset URL for a partner with embedded token
  - **Location**: `models/res_partner.py:38`
  - **Visibility**: internal
  - **Dependencies**: ir.config_parameter, werkzeug.urls

- `AuthSignupHome.web_auth_signup(*args, **kw) → response`
  - **Description**: Handles /web/signup HTTP endpoint; displays form and processes signup submission with reCAPTCHA validation
  - **Location**: `controllers/main.py:36`
  - **Visibility**: public
  - **Dependencies**: mail.template, ir.http._verify_request_recaptcha_token

- `AuthSignupHome.web_auth_reset_password(*args, **kw) → response`
  - **Description**: Handles /web/reset_password HTTP endpoint; displays form and processes password reset with reCAPTCHA validation
  - **Location**: `controllers/main.py:84`
  - **Visibility**: public
  - **Dependencies**: ir.http._verify_request_recaptcha_token

- `AuthSignupHome.web_login(*args, **kw) → response`
  - **Description**: Extends base login page to add signup/reset configuration to context
  - **Location**: `controllers/main.py:23`
  - **Visibility**: public
  - **Dependencies**: web.Home.web_login

- `AuthSignupHome.get_auth_signup_config() → dict`
  - **Description**: Retrieves signup feature flags (signup_enabled, reset_password_enabled)
  - **Location**: `controllers/main.py:124`
  - **Visibility**: internal
  - **Dependencies**: ir.config_parameter

- `AuthSignupHome.get_auth_signup_qcontext() → dict`
  - **Description**: Builds rendering context for signup/reset forms; retrieves token info if present
  - **Location**: `controllers/main.py:134`
  - **Visibility**: internal
  - **Dependencies**: res.partner._signup_retrieve_info

### Classes / Modules

- **`ResUsers`** — User model extension for signup state tracking and account creation
  - **Location**: `models/res_users.py:19`
  - **Methods**: signup, reset_password, _signup_create_user, _get_signup_invitation_scope, authenticate, _notify_inviter, _create_user_from_template, _action_reset_password, _compute_state, _search_state
  - **Extends**: res.users
  - **Dependencies**: mail.template, ir.config_parameter, ir.mail_server

- **`ResPartner`** — Partner model extension for signup token lifecycle
  - **Location**: `models/res_partner.py:24`
  - **Methods**: signup_prepare, signup_cancel, _get_signup_url, _get_signup_url_for_action, signup_get_auth_param, action_signup_prepare, _signup_retrieve_partner, _signup_retrieve_info
  - **Extends**: res.partner
  - **Fields**: signup_type (Char)
  - **Dependencies**: res.users, werkzeug.urls

- **`SignupError`** — Custom exception for signup-flow errors
  - **Location**: `models/res_partner.py:12`
  - **Purpose**: Distinguishes signup-specific exceptions from generic errors
  - **Dependencies**: None

- **`Http`** — ir.http extension for session token injection
  - **Location**: `models/ir_http.py:8`
  - **Methods**: _pre_dispatch
  - **Extends**: ir.http
  - **Dependencies**: odoo.http.request

- **`AuthSignupHome`** — HTTP controller for signup & password reset forms
  - **Location**: `controllers/main.py:20`
  - **Methods**: web_auth_signup, web_auth_reset_password, web_login, get_auth_signup_config, get_auth_signup_qcontext
  - **Extends**: web.Home
  - **Dependencies**: res.users, res.partner, ir.config_parameter, mail.template

### Constants / Configuration

- `random_token()` — Generates a 20-character random signup token (120 bits entropy) (`models/res_partner.py:15`)
- `now(**kwargs)` — Returns current datetime with optional timedelta offset (`models/res_partner.py:20`)
- `LOGIN_SUCCESSFUL_PARAMS.add('account_created')` — Adds account_created flag to login redirect params (`controllers/main.py:17`)

## Dependencies

### Internal

- **res.users** — Base user model; extended for signup state tracking and invitation handling
- **res.partner** — Base partner model; extended for signup token lifecycle
- **mail.template** — Email templates for signup confirmation and password reset links
- **ir.config_parameter** — Stores signup scope ('b2c'/'b2b'), reset_password_enabled flag, portal user template ID
- **ir.http** — Base HTTP dispatcher; extended to inject signup token/login into session
- **web.Home** — Base login page controller; extended with signup/reset form rendering

### External

- **werkzeug.urls** — URL encoding/joining for signup links with embedded tokens and redirects
- **odoo.exceptions** — UserError, AccessDenied for validation and permission failures
- **odoo.http** — HTTP routing (route decorator, Controller, request object)
- **dateutil.relativedelta** — Relative date arithmetic for token expiration calculations
- **odoo.osv.expression** — Query domain operators for state search logic

## Relationships

```mermaid
classDiagram
    class ResUsers {
        +state: Selection
        +signup(values, token) → tuple
        +reset_password(login) → action
        -_signup_create_user(values) → res.users
        -_create_user_from_template(values) → res.users
    }
    class ResPartner {
        +signup_type: Char
        +signup_prepare(signup_type) → bool
        +_get_signup_url(...) → str
        +_signup_retrieve_partner(token) → res.partner
        +_signup_retrieve_info(token) → dict
    }
    class AuthSignupHome {
        +web_auth_signup(*args) → response
        +web_auth_reset_password(*args) → response
        -get_auth_signup_config() → dict
        -get_auth_signup_qcontext() → dict
    }
    class Http {
        -_pre_dispatch(rule, args)
    }
    class SignupError {
        Exception
    }
    
    ResUsers --|> "res.users" : inherits
    ResPartner --|> "res.partner" : inherits
    AuthSignupHome --|> "web.Home" : inherits
    Http --|> "ir.http" : inherits
    AuthSignupHome --> ResUsers : calls signup/reset
    AuthSignupHome --> ResPartner : calls _signup_retrieve_info
    ResUsers --> ResPartner : links user to partner
    ResPartner --|> SignupError : raises
```

## Notes for Higher-Level Synthesis

- **Belongs with Auth component** — Extends res.users and res.partner; shares authentication flow with base login/security
- **Boundary code** — HTTP routes (/web/signup, /web/reset_password) translate form inputs to internal API calls
- **Configuration-driven** — Signup scope ('b2c'/'b2b'), reset_password_enabled are environment-controlled flags; no hardcoded feature toggles
- **Token lifecycle** — Tokens are stored in partner.signup_type field; no separate token table; expiration is implicit (partner record governs validity)
