# C4 Code Level: addons/bus

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: addons/bus
- **Content Hash**: bus-addon-root-20260701

## Overview

- **Name**: IM Bus (Instant Messaging Bus)
- **Description**: Real-time server-sent events (longpolling) infrastructure for live updates to browser clients
- **Location**: addons/bus — 34 files (root + subdirectories)
- **Language**: Python (backend) + JavaScript (frontend workers)
- **Purpose**: Provides gevent-based longpolling on port 8072; delivers real-time notifications, presence updates, and WebSocket fallback for live chat and multi-user synchronization

## Code Elements

### Module Exports

- **models** — Bus persistence, presence tracking, and model extensions (`addons/bus/models/`)
- **tools** — Utility functions and JSON serialization (`addons/bus/tools/`)
- **controllers** — HTTP endpoints for WebSocket and longpolling (`addons/bus/controllers/`)
- **websocket** — WebSocket protocol handler and frame parsing

### Root-Level Structure

The `addons/bus/` root consists of:

- **`__init__.py`** — Module initialization, imports models, tools, controllers, websocket handler
  - **Location**: `addons/bus/__init__.py:1`
  - **Exports**: `models`, `tools`, `controllers`, `websocket`

- **`__manifest__.py`** — Module metadata and asset declarations
  - **Location**: `addons/bus/__manifest__.py:1`
  - **Key Metadata**:
    - **Name**: IM Bus
    - **Category**: Hidden
    - **Version**: 1.0
    - **Dependencies**: base, web
    - **Auto-install**: True
  - **Assets**:
    - Web backend/frontend JS bundles (WebSocket worker, services)
    - Unit test bundles
    - WebSocket worker asset bundle

- **`websocket.py`** — WebSocket frame parsing and protocol logic
  - **Location**: `addons/bus/websocket.py:1`
  - **Purpose**: Low-level WebSocket frame encoding/decoding, handshake, and multiplexing
  - **Visibility**: internal
  - **Dependencies**: Python stdlib (struct, hashlib, base64)

### Subdirectory Summary

**models/** — Bus infrastructure and ORM extensions:
- `bus.Bus` — Core bus message model (channel, message, receiver)
- `bus_presence.BusPresence` — User presence tracking (online/idle/away)
- `bus_listener_mixin.BusListenerMixin` — Mixin for models to broadcast changes
- `ir_attachment.IrAttachment` — Attachment model extension (broadcast on upload)
- `ir_http.IrHTTP` — HTTP session tracking for presence
- `ir_model.IrModel` — Model change broadcasting
- `ir_qweb.IrQWeb` — Template engine extension
- `ir_websocket.IrWebsocket` — WebSocket routing and authentication
- `res_groups.ResGroups` — Group membership notifications
- `res_partner.ResPartner` — Partner model extensions (presence)
- `res_users.ResUsers` — User model extensions (presence, notification subscriptions)
- `res_users_settings.ResUsersSettings` — Per-user notification preferences

**tools/** — Utilities:
- `orjson.py` — Optimized JSON serialization wrapper

**controllers/** — HTTP/WebSocket endpoints:
- `main.py` — Primary longpolling handler (port 8072)
- `websocket.py` — WebSocket endpoint routing
- `home.py` — Home/landing page for longpolling

## Dependencies

### Internal (within Odoo)

- **base** — Core ORM and model framework
- **web** — Web framework, JavaScript assets, authentication
- Implicit: ir.model, ir.attachment, res.users, res.partner, res.groups (core Odoo tables)

### External

- **gevent** — Asynchronous I/O library (longpolling)
- **python-engineio** or compatible — WebSocket/polling transport (implicit, likely in Odoo core)
- **orjson** — Fast JSON serialization (optional, fallback to stdlib json)

## Relationship Notes

- **Core infrastructure addon**: No business logic; provides real-time plumbing for other modules
- **Auto-install**: True (always enabled, foundational for multi-user sync)
- **Hidden category**: Infrastructure-level; not visible in module list to end-users
- **Dual-protocol support**: WebSocket (modern browsers) + longpolling (fallback for older/restrictive networks)
- **Presence tracking**: Integrates with im_livechat for chat status indicators
- **ORM broadcast mixin**: Other models inherit `BusListenerMixin` to broadcast field changes

## Notes for Higher-Level Synthesis

- Pure infrastructure / utility — does NOT belong to any business domain component
- Foundational to **real-time synchronization architecture** across all multi-user features
- Boundary code: HTTP controllers expose WebSocket and longpolling endpoints; WebSocket handler translates frames to/from domain messages
- Stateful: maintains in-memory channel subscriptions, presence snapshots, and event queues
- Cross-cutting concern: extended by nearly all Odoo modules (via ORM mixins) to broadcast domain changes
