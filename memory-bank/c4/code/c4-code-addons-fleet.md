# C4 Code Level: addons/fleet (Root)

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T12:36:03Z
- **Source Directory**: addons/fleet
- **Content Hash**: root-only-inventory

## Overview

- **Name**: Odoo Fleet Management Module (Root Level)
- **Description**: Root-level module initialization and manifest for vehicle, contract, and cost tracking
- **Location**: addons/fleet — 2 files (root level only)
- **Language**: Python
- **Purpose**: Module metadata, initialization, and dependency declaration for the fleet management addon

## Code Elements

### Module Initialization

- **`models`** — Module import for fleet domain models (vehicle, contract, cost, odometer, etc.)
  - **Location**: `__init__.py:4`
  - **Purpose**: Exposes domain models defined in models/ subdirectory

- **`report`** — Module import for fleet analytics and reporting
  - **Location**: `__init__.py:5`
  - **Purpose**: Exposes reporting/analysis classes defined in report/ subdirectory

- **`wizard`** — Module import for user interaction workflows
  - **Location**: `__init__.py:6`
  - **Purpose**: Exposes wizard classes (e.g., send mail wizard) defined in wizard/ subdirectory

### Manifest Declaration

- **`__manifest__.py`** — Odoo module metadata and addon configuration
  - **Location**: `__manifest__.py:1-57`
  - **Key Fields**:
    - `name`: "Fleet"
    - `version`: "0.1"
    - `category`: "Human Resources/Fleet"
    - `depends`: `['base', 'mail']` — requires core Odoo and mail module for activity/notification
    - `data`: XML views (vehicle, model, cost, board), security, data files, wizard views
    - `demo`: `fleet_demo.xml` — sample fleet, vehicles, contracts
    - `application`: True (user-facing application)
    - `assets`: Web backend assets for vehicle management UI

## Dependencies

### Internal

- **models/** — Domain models (fleet.vehicle, fleet.vehicle.model, fleet.vehicle.log.contract, fleet.vehicle.log.services, fleet.vehicle.odometer, fleet.vehicle.cost, fleet.vehicle.tag, fleet.vehicle.state, fleet.service.type, fleet.vehicle.model.brand, fleet.vehicle.model.category, mail.activity.type, res.partner) defined in subdirectory
- **report/** — Reporting class (fleet.report) defined in subdirectory
- **wizard/** — User workflow (fleet.vehicle.send.mail) defined in subdirectory

### External

- **odoo.base** (implicit, via `depends: ['base']`) — Core Odoo models, fields, security framework
- **odoo.mail** (implicit, via `depends: ['mail']`) — Mailbox, activity, and email notification infrastructure
- **Python stdlib** — utf-8 encoding declaration

## Module Structure

- `__init__.py` — Imports `models`, `report`, `wizard` submodules (all domain classes and workflows)
- `__manifest__.py` — Addon metadata: name, version, dependencies, security files, view XML, data files, demo data
- **Subdirectories (not examined in root-only inventory)**:
  - `models/` — 14 model classes (vehicle, contract, cost, odometer, service logs, categories, brands, tags, states, partner extensions)
  - `report/` — Fleet cost and analytics reporting
  - `wizard/` — Send email wizard for vehicle notifications
  - `security/` — RBAC definitions (fleet_security.xml, ir.model.access.csv)
  - `data/` — XML data files, vehicle templates, activity types, mail subtypes
  - `views/` — View definitions for vehicles, costs, models, board dashboard
  - `tests/` — Test suites (access rights, overdue contracts)
  - `static/` — Web frontend assets

## Notes for Higher-Level Synthesis

- **Boundary Module**: Fleet is a distinct HR/Operations domain (vehicle lifecycle, contracts, costs, driver management) that depends on mail for activity/notification but owns its own comprehensive data model
- **Belongs with**: Domain component group (alongside maintenance, helpdesk when present) — shares mail dependency and uses similar activity/notification patterns; vehicle records are referenced by maintenance (equipment tracking) and possibly accounting (cost allocation)
- **Reporting Integration**: Has a dedicated reporting class for cost analysis and fleet metrics — may feed into analytics or dashboard aggregation
- **Pure domain code**: No HTTP boundaries, no utility code — this is a full application module with views, wizards, reporting, and security
