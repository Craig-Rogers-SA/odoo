# C4 Code Level: Gamification Addon

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: addons/gamification (root only)
- **Content Hash**: `addons-gamification-root-20260701`

## Overview

- **Name**: Gamification Addon (Root Package)
- **Description**: Root package module and manifest for Odoo 18 Gamification module. Exports models and wizard subpackages.
- **Location**: `addons/gamification/` — 2 files (root level only)
- **Language**: Python 3 (Odoo)
- **Purpose**: Provides motivational framework (goals, challenges, badges, karma) to evaluate and incentivize Odoo users across HR, CRM, and helpdesk applications.

## Code Elements

### Module Package

- **`gamification`** — Root package initialization and manifest for Gamification module
  - **Location**: `__init__.py:<1>`, `__manifest__.py:<1>`
  - **Module exports**: `models` (subpackage), `wizard` (subpackage)
  - **Addon metadata**:
    - Name: `Gamification`
    - Version: `1.0`
    - Category: `Human Resources`
    - Sequence: `160`
    - License: `LGPL-3`

### Functions / Methods

No top-level functions defined at root level. Implementation split across subdirectories (`models/`, `wizard/`, `tests/`).

### Key Data Files

Configuration and data files defined in manifest (loaded by Odoo framework):
- Models: `gamification_badge`, `gamification_challenge`, `gamification_goal`, `gamification_karma_rank`, `gamification_karma_tracking`, `res_users` (extended)
- Wizards: `grant_badge`, `update_goal`
- Views: Badge views, challenge views, goal views, karma tracking views, karma rank views, menu structure
- Security: Model-level ACLs via `gamification_security.xml` and `ir.model.access.csv`
- Cron jobs: Scheduled goal/challenge updates
- Email templates: Challenge report mail templates

## Dependencies

### Internal (Odoo Modules)

- `mail` — Required dependency for mail.thread integration (goals, badges, challenge updates send notifications)

### External

- **Python standard library** (implicit via Odoo framework)
- **phonenumbers** — Not used at gamification root (used by phone_validation addon)

### Subpackages (Root Directory Imports)

- `models` — Object-relational mapping (ORM) models: Badge, Challenge, Goal, Karma tracking
- `wizard` — Transient wizard forms: Grant badge, Update goal workflows

## Relationships

No direct relationships defined at root level. Cross-module dependencies:

- **Consumed by**: `crm`, `helpdesk`, `hr` modules (via mail.thread integration to trigger goals/badges on actions)
- **Extends**: `res.users` model with karma_rank_id and karma fields

## Notes for Higher-Level Synthesis

- **Domain-bearing core**: Gamification is a standalone motivational system that integrates with CRM (sales targets), HR (attendance goals), and helpdesk (satisfaction). Models in subdirectories (`models/`) form the domain layer; this root declares module-level interfaces.
- **Wizard-based workflows**: Badge granting and goal updating are transient wizard operations; implement user-facing workflows for manual administrative tasks.
- **Shared user extension**: `res_users` is extended (karma tracking) — should group with Auth/User Management component at synthesis time.
- **No boundary handlers**: No HTTP controllers at root; user interaction is via views (Odoo form UI). Webhook/notification triggers handled by mail.thread integration.

