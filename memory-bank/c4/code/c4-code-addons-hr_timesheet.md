# C4 Code Level: hr_timesheet (Task Logs & Time Tracking)

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: addons/hr_timesheet (root only)
- **Content Hash**: root-level-files-only

## Overview

- **Name**: Timesheet Module (Task Logs)
- **Description**: Tracks employee time spent on projects and tasks; extends account.analytic.line with timesheet-specific fields (unit_amount, employee_id, task_id, project_id)
- **Location**: `addons/hr_timesheet/` — 2 files at root level
- **Language**: Python (Odoo 18.0)
- **Purpose**: Core timesheet system for project time tracking; integrates employee timesheets with project management, cost accounting, and reporting

## Code Elements

### Root Module Initialization

- `create_internal_project(env)` — Post-install hook: create internal project and initial timesheet entries
  - **Description**: Initializes internal project for admin user at module installation; flags projects to allow timesheets
  - **Location**: `addons/hr_timesheet/__init__.py:15`
  - **Visibility**: public (post_init_hook)
  - **Dependencies**: Odoo framework (env.ref, env['project.project'], env['account.analytic.line']), fields.datetime

- `_pre_init_hook(env)` — Pre-installation hook: add computed columns to avoid OOM on large databases
  - **Description**: Manually creates task_id, parent_task_id, project_id, department_id, manager_id columns on account_analytic_line before computed fields trigger
  - **Location**: `addons/hr_timesheet/__init__.py:53`
  - **Visibility**: public (pre_init_hook)
  - **Dependencies**: Odoo SQL tools (env.cr.execute, SQL), database schema manipulation

- `_uninstall_hook(env)` — Cleanup on module removal
  - **Description**: Archives internal projects and removes default stage data when timesheet module is uninstalled
  - **Location**: `addons/hr_timesheet/__init__.py:35`
  - **Visibility**: public (uninstall_hook)
  - **Dependencies**: env.ref, ir.model.data search/unlink

### Root Configuration

- **`__manifest__.py`** — Module metadata and dependencies
  - **Location**: `addons/hr_timesheet/__manifest__.py:1`
  - **Depends**: `hr`, `hr_hourly_cost`, `analytic`, `project`, `uom`
  - **Purpose**: Declares hr_timesheet as installed application; loads timesheet views, project/task extensions, portal templates, and reporting
  - **Key Assets**:
    - Views: hr_timesheet_views.xml, project_project_views.xml, project_task_views.xml, project_update_views.xml
    - Reports: hr_timesheet_report_view.xml, project_report_view.xml, timesheet_templates.xml
    - Portal: hr_timesheet_portal_templates.xml, project_task_portal_templates.xml
    - Wizards: hr_employee_delete_wizard_views.xml
    - Security: hr_timesheet_security.xml, ir.model.access.csv/xml

## Key Models (Subdirectory Summary)

Root-level only — detailed model signatures in `models/` subdirectory:

- **`account.analytic.line`** (extended) — Core timesheet entry record
  - **Fields**:
    - `task_id: Many2one(project.task)` — Task being worked on
    - `project_id: Many2one(project.project)` — Project context (computed from task)
    - `employee_id: Many2one(hr.employee)` — Employee who logged time
    - `unit_amount: Float` — Hours/time units spent
    - `department_id: Many2one(hr.department)` — Employee's department (computed)
    - `manager_id: Many2one(hr.employee)` — Employee's manager (computed)
    - `parent_task_id: Many2one(project.task)` — Parent task reference (computed)
    - `encoding_uom_id: Many2one(uom.uom)` — Unit of measure (hours, days, etc.)
    - `readonly_timesheet: Boolean` — Portal user read-only flag
  - **Location**: `models/hr_timesheet.py:14` (extensive)
  - **Key Methods**:
    - `_get_favorite_project_id(employee_id)` — Returns most-recently-used project
    - `default_get(field_list)` — Auto-populate employee, project on new entry
    - `_domain_project_id()` — Filter projects by visibility/access
    - `_domain_employee_id()` — Filter employees by approval role
    - `_compute_*()` — Auto-compute task, project, department, manager fields

- **`project.project`** (extended) — Projects now support timesheet mode
  - **Location**: `models/project_project.py`
  - **Key Additions**: `allow_timesheets: Boolean`, timesheet reporting

- **`project.task`** (extended) — Tasks now track time
  - **Location**: `models/project_task.py`
  - **Key Additions**: timesheet validation, task-level time aggregation

- **`hr.employee`** (extended) — Employees now have timesheet history
  - **Location**: `models/hr_employee.py`
  - **Key Additions**: timesheet reporting, hourly cost

- **`uom.uom`** (extended) — Company-wide timesheet encoding unit
  - **Location**: `models/uom_uom.py`
  - **Key Addition**: `timesheet_encode_uom_id` on company

### Portal & Reporting Controllers

- **`controllers/portal.py`** — Portal view for employee timesheets
  - **Purpose**: Allow portal users to view/edit their own timesheet entries

- **`controllers/project.py`** — Project timesheet portal integration
  - **Purpose**: Time tracking UI for project collaborators

## Dependencies

### Internal

- **`project` module** — project.project, project.task, project.collaborator, project.milestone, project.update
  - Used for: Task/project context, project visibility, collaborator access control
  - Example: `project_id` and `task_id` fields on analytic line

- **`hr` module** — hr.employee, hr.department
  - Used for: Employee master data, department-level timesheet aggregation, manager hierarchy

- **`analytic` module** — account.analytic.line (base model extended here)
  - Used for: Cost accounting, analytic accounts, financial reporting

- **`hr_hourly_cost` module** — Employee hourly cost for cost tracking
  - Used for: Time-to-cost conversion in reporting

- **`uom` module** — Unit of measure for time encoding
  - Used for: Hours, days, weeks encoding flexibility

### External

- **`python-dateutil`** — Relative date calculations
  - Purpose: Timesheet period filtering, rolling date ranges

- **`statistics.mode()`** — Python stdlib
  - Purpose: Most-frequent project selection in `_get_favorite_project_id()`

- **`re`** — Regex library
  - Purpose: Field validation patterns (if used in models)

- **`collections.defaultdict`** — Default dictionary containers
  - Purpose: Grouping timesheets by various dimensions (project, task, employee)

## Relationships

```mermaid
classDiagram
    class AccountAnalyticLine {
        +task_id: Many2one(project.task)
        +project_id: Many2one(project.project)
        +employee_id: Many2one(hr.employee)
        +unit_amount: Float
        +date: Date
        +department_id: Many2one(hr.department)
        +manager_id: Many2one(hr.employee)
        _compute_task_id()
        _compute_project_id()
        _compute_department_id()
        _get_favorite_project_id()
    }
    
    class ProjectTask {
        +project_id: Many2one
        +allow_timesheets: Boolean
    }
    
    class ProjectProject {
        +allow_timesheets: Boolean
        +internal_project_id: Boolean
    }
    
    class HrEmployee {
        +hourly_cost: Monetary
    }
    
    AccountAnalyticLine -->|tracks time on| ProjectTask
    AccountAnalyticLine -->|in project| ProjectProject
    AccountAnalyticLine -->|logged by| HrEmployee
    ProjectTask -->|parent| ProjectTask
    ProjectProject -->|has| ProjectTask
```

## Notes for Higher-Level Synthesis

- **Belongs with Project component** — Extends project.project and project.task; tight coupling with project management
- **Also integral to HR component** — Employee timesheet history, cost tracking; integrates with payroll (hr_hourly_cost)
- **Portal boundary**: Controllers handle portal user access; separate read-only mode for external collaborators
- **Cost accounting link**: account.analytic.line bridge connects time tracking to financial GL
- **Pre-install hook critical**: Adds computed columns to avoid OOM on fresh install of large databases (1000s of existing analytic lines)
- **Project visibility pattern**: Domain filters enforce project.privacy_visibility and message_partner_ids access control
