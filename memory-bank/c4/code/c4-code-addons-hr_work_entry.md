# C4 Code Level: hr_work_entry (Work Entries)

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: addons/hr_work_entry
- **Content Hash**: 3206726d5879e2fb

## Overview

- **Name**: Work Entries Module
- **Description**: Bootstrap and module orchestration for Odoo Work Entries addon (hr_work_entry)
- **Location**: addons/hr_work_entry — 2 files (root level only), ~27 lines
- **Language**: Python
- **Purpose**: Root-level module initialization and manifest configuration for the Work Entries feature set. Provides foundational hr.work.entry records (derived from attendance, leave, and contract schedules) used by payroll to compute worked/overtime/leave days.

## Code Elements

### Module Exports

- **`__init__.py`** — Module initialization bootstrap
  - **Location**: `addons/hr_work_entry/__init__.py:<1>`
  - **Visibility**: internal
  - **Imports**: `models` subpackage
  - **Purpose**: Registers hr.work.entry and related models (hr.employee, resource.calendar extensions) so Odoo can discover them at addon load time

- **`__manifest__.py`** — Addon metadata and manifest
  - **Location**: `addons/hr_work_entry/__manifest__.py:<1>`
  - **Visibility**: internal
  - **Purpose**: Declares addon name (Work Entries), dependencies (hr), and data/view loading order. Marks addon as installable, provides foundational records for payroll processing.

### Manifest Configuration

- `'name'` — Work Entries (human-readable addon name)
- `'category'` — Human Resources/Employees
- `'sequence'` — 39 (load order in menu, early in HR chain)
- `'summary'` — Manage work entries
- `'installable'` — True (available for installation)
- `'license'` — LGPL-3

### Key Dependencies (from manifest)

- `hr` — Base human resources model (Employee, etc.)

### Assets & Data Files

- **Security**: hr_work_entry_security.xml, ir.model.access.csv
- **Data**: hr_work_entry_data.xml (initial work entry records/rules)
- **Views**: hr_work_entry_views.xml, hr_employee_views.xml, resource_calendar_views.xml
- **Frontend Assets**: Static web assets (JavaScript/SCSS) under static/

## Dependencies

### Internal

- `models` subpackage — Contains hr.work.entry (core record type), hr.employee extensions, and resource.calendar extensions

### External

- `hr` (Odoo core) — Base Employee and Department models

## Relationships

```
hr_work_entry/
  __init__.py ──→ [models/] ──→ hr.work.entry, hr.employee (extended), resource.calendar (extended)
```

Root initialization imports `models` package, making hr.work.entry records discoverable by Odoo ORM at addon startup. Used as foundation for downstream payroll and contract-driven work entry generation (hr_work_entry_contract).

## Notes for Higher-Level Synthesis

- **Foundation Layer**: This addon provides the base hr.work.entry record type and associated views/security. It does not compute work entries; that is done by hr_work_entry_contract (which depends on hr_work_entry).
- **Payroll Dependency**: Work entries (hours worked, overtime, leave) are inputs to payroll computation. This module's records are the canonical source of truth for worked time.
- **Belongs With**: hr_work_entry_contract (extends this with contract-driven computation), hr_payroll (consumes work entries to compute pay), hr_attendance (raw attendance records that feed work entry computation).
- **Pure Data + Views**: This addon itself has no complex business logic at the root level; models and wizard subpackages contain the real functionality.
