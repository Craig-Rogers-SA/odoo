# C4 Code Level: addons/iap

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: addons/iap
- **Content Hash**: iap-addon-root-20260701

## Overview

- **Name**: In-App Purchases (IAP)
- **Description**: Framework for credit-based in-app services (SMS, OCR, email enrichment, lead mining)
- **Location**: addons/iap — 10 files (root + subdirectories)
- **Language**: Python (backend) + JavaScript (frontend)
- **Purpose**: Provides account model and RPC helpers for Odoo IAP service integration; manages credit top-ups, service subscriptions, and billing against Odoo IAP platform (iap.odoo.com)

## Code Elements

### Module Exports

- **models** — IAP account and service discovery models (`addons/iap/models/`)
- **tools** — RPC helpers and charge/authorize/capture payment flows (`addons/iap/tools/`)

### Root-Level Structure

The `addons/iap/` root consists of:

- **`__init__.py`** — Module initialization with compatibility imports
  - **Location**: `addons/iap/__init__.py:1`
  - **Exports**: 
    - `models`, `tools` (direct imports)
    - **Compatibility layer** (backward-compat re-exports):
      - `iap_jsonrpc` (aliased as `jsonrpc`)
      - `iap_authorize`
      - `iap_cancel`
      - `iap_capture`
      - `iap_charge`
      - `InsufficientCreditError` (exception class)
  - **Visibility**: public
  - **Purpose**: Expose IAP transaction helpers to consuming modules (e.g., sms, snailmail)

- **`__manifest__.py`** — Module metadata and asset declarations
  - **Location**: `addons/iap/__manifest__.py:1`
  - **Key Metadata**:
    - **Name**: In-App Purchases
    - **Category**: Hidden/Tools
    - **Version**: 1.1
    - **Dependencies**: web, base_setup
    - **Auto-install**: True
  - **Assets**:
    - Web backend JavaScript UI components for credit display and top-up dialogs

### Subdirectory Summary

**models/** — IAP domain models:
- `iap_account.IapAccount` — User credit account (balance, top-up history, linked Odoo account)
- `iap_service.IapService` — Service registry (SMS, OCR, enrichment, lead mining; price per unit)
- `iap_enrich_api.IapEnrichApi` — Partner enrichment service integration (B2B company data, emails)

**tools/** — RPC and transaction helpers:
- `iap_tools.py` — Core functions:
  - `iap_jsonrpc(service_name, method, *args, **kwargs)` — Call Odoo IAP RPC endpoint
  - `iap_authorize(service_name, amount)` → `transaction_id` — Pre-authorize charge
  - `iap_capture(transaction_id)` — Finalize charge
  - `iap_cancel(transaction_id)` — Reverse authorization
  - `iap_charge(service_name, amount, description)` — Atomic charge (authorize + capture)
  - `InsufficientCreditError` — Raised when account balance < amount
  - Context manager helpers for transactional semantics

## Dependencies

### Internal (within Odoo)

- **web** — Web framework, JavaScript assets, authentication
- **base_setup** — Base configuration and company settings
- Implicit: res.users (account owner), res.company (billing entity)

### External

- **Odoo IAP Platform** (`iap.odoo.com:443`) — Remote HTTP RPC endpoint
  - Hosts SMS, OCR, lead mining, partner enrichment services
  - Returns JSON-RPC responses; credit management API
- **requests** (or urllib3) — HTTP client for IAP calls (implicit, part of Odoo core)

## Relationship Notes

- **Platform/utility addon**: Provides payment rails for other modules (sms, snailmail, hr_org_chart, …)
- **Auto-install**: True (foundational for any IAP-dependent module)
- **Hidden category**: Infrastructure; not visible to end-users
- **Credit-based billing**: All charges deducted from user's IAP account; no per-transaction invoicing
- **RPC bridge**: Translates Odoo domain calls → IAP JSON-RPC → remote SaaS
- **Backward-compatibility exports**: Root `__init__.py` re-exports tools for legacy code paths

## Notes for Higher-Level Synthesis

- Pure infrastructure / platform adapter — does NOT belong to any business domain component
- Service catalog pattern: IapService registry allows dynamic discovery of available SaaS capabilities
- Boundary code: `iap_tools.py` translate between Odoo transaction semantics and remote IAP platform RPC
- Error handling: InsufficientCreditError signals out-of-credit conditions; callers must handle gracefully
- Security context: Transactions tied to authenticated user and company; quota enforcement on IAP platform
