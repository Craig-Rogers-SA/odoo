# C4 Code Level: addons/im_livechat

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: addons/im_livechat
- **Content Hash**: root-level-py-files-only

## Overview

- **Name**: Live Chat Module
- **Description**: Odoo 18.0 Live Chat addon providing website visitor chat widget, operator assignment, chatbot automation, and session analytics
- **Location**: addons/im_livechat — 2 root-level files (57 total files in addon tree including models, controllers, reports, tools)
- **Language**: Python (Odoo framework)
- **Purpose**: Operates real-time chat support infrastructure: visitor engagement (website widget), operator management (availability, assignment), chatbot scripting (pre-canned responses), session history, CSAT ratings, and analytics reporting

## Code Elements

### Module Structure

- **`im_livechat`** — Root package initialization
  - **Location**: `__init__.py:<1-6>`
  - **Imports**:
    - `from . import controllers` — WebSocket/HTTP endpoints for chat widget, chatbot messages, RTC (real-time communication)
    - `from . import models` — Domain models (im_livechat_channel, chatbot_script, discuss_channel overrides)
    - `from . import report` — Analytics views (channel report, operator report)
    - `from . import demo` — Demo data fixtures
    - `from . import tools` — Utility functions (misc.py — helper functions)
  - **Visibility**: Package root; all submodules are internal

### Manifest / Configuration

- **`__manifest__.py`** — Module metadata and asset declarations
  - **Location**: `__manifest__.py:<1-162>`
  - **Key Properties**:
    - Name: "Live Chat"
    - Version: 1.0
    - Category: Website/Live Chat
    - Depends: `mail`, `rating`, `digest`, `utm` (for campaign tracking)
    - Installs 19 data/view/template files + 11 demo sessions
  - **Asset Bundles**:
    - `web.assets_frontend` — Chat widget JS/CSS (embed assets)
    - `web.assets_backend` — Admin channel/chatbot/operator management UI
    - `im_livechat.assets_embed_core` — Shared embed library code (common to all embed variants)
    - `im_livechat.assets_embed_external` — External-origin chat widget (CORS-safe variant)
    - `im_livechat.assets_embed_cors` — CORS-enabled variant for cross-domain widget
    - `mail.assets_public` — Public web chat UI components
    - **Test bundles**: qunit_embed_suite, assets_unit_tests, assets_tests

## Dependencies

### Internal

- `models/` (not enumerated at root level — contains im_livechat_channel, chatbot_script, chatbot_script_step, chatbot_script_answer, chatbot_message, discuss_channel/member overrides, mail_message overrides, res_users/res_users_settings overrides, rating_rating/digest overrides)
- `controllers/` (not enumerated at root level — HTTP/WS endpoints for chat widget, CORS variants, RTC, thread mgmt, webclient integration)
- `report/` (not enumerated at root level — channel analytics, operator performance reports)
- `tools/` (not enumerated at root level — utility functions)

### External

- `mail` — Base messaging infrastructure, message storage, user messaging
- `rating` — CSAT rating system (post-session satisfaction surveys)
- `digest` — Scheduled digest reports (operator statistics, channel summaries)
- `utm` — Campaign/source tracking for visitor attribution
- Odoo Web Framework (assets: Bootstrap, Owl.js, FontAwesome, WebSocket via Bus)

## Status

**Root-level inventory only**: This directory contains only module initialization and manifest. All substantive code (models, controllers, reports, tools) resides in subdirectories. The c4-component agent will synthesize subdirectories into logical components during the next level.

## Notes for Higher-Level Synthesis

- **Boundary code**: Controllers in `controllers/` expose HTTP routes (chat widget bootstrap, WebSocket upgrade, chatbot API) and CORS variants — strong website boundary marker
- **Widget architecture**: Three embed asset bundles (core, external, cors) indicate a sophisticated multi-origin deployment model (same-domain, external CDN, and CORS-wrapped variants) — this is critical for deployment topology understanding
- **Automation layer**: Chatbot models (chatbot_script, chatbot_script_step, chatbot_script_answer) with chatbot_message dispatch are a distinct automation subcomponent (rules engine for pre-routing)
- **Visitor tracking**: im_livechat_channel, discuss_channel overrides, and visitor models suggest session-scoped visitor identity separate from authenticated users — important for anonymous browsing support
- **Analytics**: report/ subdir indicates both real-time channel metrics and historical operator performance — may warrant a separate "Analytics" component
- **Related components**: Tightly coupled with `mail` (messaging backend), `rating` (satisfaction tracking), `digest` (reporting) — map these as external integrations; loose coupling with `utm` (optional visitor source tracking)
