# C4 Code Level: Link Tracker (`addons/link_tracker`)

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: `addons/link_tracker`
- **Content Hash**: `8ccfb3f28ec8f4339fb9302880d1759fb897b652af9603d7d0d2d0e5e2f73182`

## Overview

- **Name**: Link Tracker
- **Description**: URL shortener with click-tracking and UTM parameter management for marketing campaigns.
- **Location**: `addons/link_tracker` — 2 files, ~60 lines
- **Language**: Python (Odoo XML/Python module)
- **Purpose**: Provides URL shortening and click tracking capabilities for mass mailing campaigns, enabling attribution of link clicks to campaigns via unique shortened URLs and optional UTM parameters.

## Code Elements

### Modules

- **`controller`** — HTTP request handlers for tracking link clicks and redirects
  - **Location**: Imported in `__init__.py:<4>`
  - **Purpose**: Handles incoming click traffic, logs analytics, and redirects to original URLs
  - **Dependencies**: Odoo HTTP routing, link.tracker model

- **`models`** — Domain models for link tracking
  - **Location**: Imported in `__init__.py:<5>`
  - **Purpose**: Defines `link.tracker` model (shortened URLs, click counts, UTM mappings) and interactions with utm.campaign
  - **Dependencies**: Odoo ORM (inherited from mail, utm modules)

- **`tools`** — Utility functions for link shortening and parameter handling
  - **Location**: Imported in `__init__.py:<6>`
  - **Purpose**: Helper methods for encoding/decoding shortened URLs, managing URL parameters, generating tracking codes
  - **Dependencies**: Odoo utilities

### Manifest Configuration

- **Module Name**: `Link Tracker` (line 5)
  - **Category**: Marketing
  - **Version**: 1.1
  - **Dependencies**: 
    - `utm` — UTM campaign and source/medium/campaign tracking (Odoo built-in)
    - `mail` — Email integration, mailing list foundation (Odoo built-in)
  - **Data Files**:
    - `views/link_tracker_views.xml` — UI for shortened link management
    - `views/utm_campaign_views.xml` — Campaign configuration integration
    - `security/ir.model.access.csv` — Access control

## Dependencies

### Internal (to Odoo modules)

- **`utm`** — UTM campaign and parameter source; link_tracker extends utm.campaign views to display tracked clicks
- **`mail`** — Mailing foundation; link_tracker provides click tracking for mail.mass_mailing campaigns

### External

- No third-party Python packages declared; uses only Odoo built-in framework

## Architecture Notes

- **Boundary Code**: This is a **boundary module** — it handles HTTP request entry points (click redirect) and sits between external link clicks and internal campaign/mailing models.
- **Integration Point**: Core integration with `mass_mailing` module (not directly imported in root, but the purpose is to serve mass_mailing campaigns with tracking).
- **Model-Centric**: The actual tracking logic is in the models (likely `link.tracker` ORM model with onClick handler).

## Notes for Higher-Level Synthesis

- **Shared Concept**: Belongs with the `mass_mailing` component — link tracking is one of the core analytics features for mailing campaigns. Also integrates closely with `utm` and `sale` (for campaign ROI tracking in sales).
- **Boundary**: HTTP entry point for external redirect traffic — separates public click events from internal analytics.
- **Minimal Root-Level Code**: Most logic is in models and controllers subdirectories (not documented at root level per C4 scope). Root `__init__.py` is module initialization only.
