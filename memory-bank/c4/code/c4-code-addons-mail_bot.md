# C4 Code Level: addons/mail_bot

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: addons/mail_bot
- **Content Hash**: a7c1e5f8b2d4e9a3f6c1b8d2

## Overview

- **Name**: OdooBot / Mail Bot
- **Description**: Virtual assistant that automates onboarding welcome messages and channel member management
- **Location**: addons/mail_bot — 2 root files
- **Language**: Python (Odoo 18.0)
- **Purpose**: Extends mail/discuss functionality with automated bot responses to new users and contextual channel notifications

## Code Elements

### Root Module Structure

- **`__init__.py`** — Module initialization loader
  - **Location**: `addons/mail_bot/__init__.py:1`
  - **Visibility**: public
  - **Dependencies**: Imports `models` and `controllers` submodules
  - **Purpose**: Loads bot model definitions and HTTP request handlers

- **`__manifest__.py`** — Module metadata and configuration
  - **Location**: `addons/mail_bot/__manifest__.py:1`
  - **Visibility**: public
  - **Purpose**: Declares OdooBot addon v1.2 with mail dependency; auto-installs on mail presence

### Key Dependencies (from __manifest__.py)

**Internal Odoo Addons:**
- `mail` — Core mail/discuss infrastructure (discuss.channel, mail.message, mail.alias)

**Configuration & Assets:**
- **Auto-install**: True (installs automatically when mail is installed)
- **Views**: `views/res_users_views.xml` — Bot user profile configuration
- **Data**: `data/mailbot_data.xml` — Bot instance definition and default responses
- **Assets**: `mail_bot/static/src/scss/odoobot_style.scss` — Bot-specific styling

**Models Implied (from submodule structure):**
- `mail_bot.mail_bot` — Bot instance and configuration
- `discuss.channel` — Channel membership and bot assignment
- `res.users` — OdooBot user entity with special privileges
- `mail.message` — Message creation and posting on behalf of bot

**Features Implied:**
- Automated welcome message to new users
- Channel member notification when users join/leave
- Canned response dispatch based on message keywords
- Integration with discuss.channel for real-time bot messages
- Special user account (OdooBot) with restricted permissions

## Dependencies

### Internal

- `mail` — Core mail gateway, discuss channels, mail.message, and real-time bus infrastructure

### External

- None declared at root level (all dependencies are internal Odoo addons)

## Relationships

The mail_bot addon is a **lightweight automation layer** on top of the mail/discuss ecosystem:

```
mail_bot (root)
├── models/ — Bot configuration and channel integration
├── controllers/ — HTTP handlers for bot actions and channel events
└── static/src/scss/ — Bot UI styling
```

**Interaction Pattern:**
- Listens to `discuss.channel.member.create` and `res.users` events
- Dispatches `mail.message.create` on behalf of OdooBot user
- Triggered by channel membership changes and keyword pattern matching

## Notes for Higher-Level Synthesis

- **Lightweight Automation Addon**: Does not define new business models; extends mail/discuss with bot behavior. Synthesize as part of "Messaging & Discuss" component alongside mail addon.
- **Auto-Install Dependency**: Marked auto-install; always present when mail is installed. Consider it mandatory for the Discuss component architecture.
- **Boundary Code**: Controllers handle chat integration webhooks and real-time bot responses—interaction point between backend automation and user-facing chat UI.
- **Low Coupling**: Minimal internal state; primarily orchestrates mail and channel APIs. Good dependency isolation.
