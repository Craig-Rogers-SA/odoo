# C4 Code Level: Phone Validation Addon

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: addons/phone_validation (root only)
- **Content Hash**: `addons-phone_validation-root-20260701`

## Overview

- **Name**: Phone Validation Addon (Root Package)
- **Description**: Root package module and manifest for Odoo 18 Phone Number Validation module. Exports lib (phonenumbers wrapper), tools (validation API), models, and wizard subpackages.
- **Location**: `addons/phone_validation/` — 2 files (root level only)
- **Language**: Python 3 (Odoo)
- **Purpose**: Validates, formats, and normalizes international phone numbers (res.partner, hr.employee, mail.thread) using phonenumbers library. Manages phone blacklist for spam/complaint tracking.

## Code Elements

### Module Package

- **`phone_validation`** — Root package initialization and manifest for Phone Numbers Validation module
  - **Location**: `__init__.py:<1>`, `__manifest__.py:<1>`
  - **Module exports**: `lib` (subpackage), `tools` (subpackage), `models` (subpackage), `wizard` (subpackage)
  - **Addon metadata**:
    - Name: `Phone Numbers Validation`
    - Version: `2.1`
    - Category: `Hidden` (utilities module)
    - Sequence: `9999` (loads early in dependency chain)
    - License: `LGPL-3`
    - Auto-install: `True` (required by many modules)

### Functions / Methods

No top-level functions defined at root level. Implementation split across subdirectories:
- `lib/` — phonenumbers library wrapper and regional metadata patches
- `tools/` — Validation/formatting API functions
- `models/` — ORM model mixins and extensions
- `wizard/` — Phone blacklist management workflow

### Key Data Files

Configuration and data files defined in manifest (loaded by Odoo framework):
- Models: `phone.blacklist` (spam/complaint phone registry), `mail.thread.phone` (mixin for sanitizing phone fields), `res.partner` (extended), `res.users` (extended)
- Wizards: `phone_blacklist_remove` (UI to remove from blacklist)
- Views: Phone blacklist list/form views, mail thread phone handling
- Security: Model ACLs via `ir.model.access.csv`

## Dependencies

### Internal (Odoo Modules)

- `base` — Core Odoo framework (res.partner, res.users models)
- `mail` — Mail thread integration (mail.thread.phone mixin for message sanitization)

### External

- **`phonenumbers`** (third-party library) — Google's international phone number parsing, formatting, and validation library. Vendored in `lib/` with regional metadata patches for Brazil, Côte d'Ivoire, Colombia, Israel, Kenya, Morocco, Mauritius, Panama, Senegal (country-specific formatting rules).

### Subpackages (Root Directory Imports)

- `lib` — Wrapper around phonenumbers library; phonemetadata.py (region data), phonenumbers_patch/ (regional customizations)
- `tools` — Validation/formatting utilities: `phone_validation.py` (core API: `validate_phone`, `format_phone`, `parse_phone`)
- `models` — ORM model mixins and field implementations: `mail_thread_phone` (thread sanitization), `phone_blacklist` (spam registry), `res_partner` (phone field methods), `res_users` (phone field methods)
- `wizard` — Transient forms: `phone_blacklist_remove` (remove phone from blacklist)

## Relationships

No direct relationships defined at root level. Cross-module dependencies:

- **Consumed by**: `crm`, `helpdesk`, `hr`, `sale`, `purchase`, `account`, `website` (any module with phone fields on res.partner, hr.employee, or mail.thread)
- **Extends**: `res.partner` (add phone validation/formatting), `res.users` (add phone field), `mail.thread` (add phone sanitization)
- **Extends models**: Any model inheriting from `mail.thread.phone` mixin gains phone sanitization

## Notes for Higher-Level Synthesis

- **Utility/infrastructure module**: Phone validation is a horizontal cross-cutting utility (not domain-bearing). It enhances phone field handling across many modules. At synthesis, should NOT group with domain components; instead, mark as infrastructure/shared-utilities layer.
- **Auto-install**: Designed to be auto-installed (sequence 9999, auto_install=True); all dependent modules assume it's present. Must be loaded early in the addon dependency chain.
- **Regional customization**: Phonenumbers library has Google-maintained region data; addon patches specific countries (Brazil, Côte d'Ivoire, Colombia, Israel, Kenya, Morocco, Mauritius, Panama, Senegal) to override defaults. Maintenance point: new regions may require patches in `lib/phonenumbers_patch/`.
- **Mixin-based extension**: Provides `mail.thread.phone` mixin; models that need phone sanitization inherit from it. Also extends res.partner and res.users directly (not via mixin).
- **No boundary handlers**: No HTTP controllers; all integration is ORM-level (field validation, formatting on save).
- **Blacklist as domain model**: `phone.blacklist` is a small domain model (spam/complaint tracking); could logically belong to CRM or support module at synthesis time, but defined here for reusability.

