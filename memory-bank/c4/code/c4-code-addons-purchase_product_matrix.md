# C4 Code Level: addons/purchase_product_matrix

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: addons/purchase_product_matrix
- **Content Hash**: root-files-only

## Overview

- **Name**: Purchase Product Matrix
- **Description**: Grid-based interface for purchasing products with variants (size, color, etc.) within a single purchase order line
- **Location**: addons/purchase_product_matrix — 2 root files
- **Language**: Python (Odoo ORM), JavaScript (frontend)
- **Purpose**: Enables rapid purchase order creation by allowing buyers to select product variants and quantities in a matrix grid view, reducing order entry time for multi-variant products

## Code Elements

### Modules

- **`purchase_product_matrix` package** — Odoo addon extending Purchase module with matrix variant selection
  - **Location**: `__init__.py:1`
  - **Imports**: 
    - `models` (subpackage: Purchase order and line model extensions for matrix entry)
  - **Initialization Purpose**: Registers model extensions when addon is loaded

## Dependencies

### Internal (Odoo Modules)

- `purchase` — Provides `purchase.order` and `purchase.order.line` models
- `product_matrix` — Provides grid/matrix UI infrastructure and variant handling

### External

- `odoo` (base framework) — ORM, model inheritance, form views
- JavaScript/Web assets — Frontend matrix grid component

## Package Manifest

**File**: `__manifest__.py`

- **Name**: Purchase Matrix
- **Summary**: Add variants to your purchase orders through an Order Grid Entry
- **Version**: 1.0
- **Category**: Inventory/Purchase
- **Depends**: `['purchase', 'product_matrix']`
- **License**: LGPL-3

**Data Files Loaded**:
- `views/purchase_views.xml` — Purchase order and line view definitions with matrix variant controls
- `report/purchase_quotation_templates.xml` — Purchase quotation report template
- `report/purchase_order_templates.xml` — Purchase order report template

**Frontend Assets** (`web.assets_backend`):
- `purchase_product_matrix/static/src/**/*` — Matrix grid component JavaScript and styles

**Test Assets** (`web.assets_tests`):
- `purchase_product_matrix/static/tests/tours/**/*` — Automated UI test tours

## Root-Level Architecture

The root level consists of:
1. **Package initialization** (`__init__.py`) — imports models subpackage
2. **Addon manifest** (`__manifest__.py`) — metadata, dependencies, data files, web assets, test tours

**Note**: The actual functionality (model extensions for matrix grid support, view definitions, report templates) lives in subdirectories (`models/`, `views/`, `report/`, `static/`). This root-level walk captures only the package structure and asset configuration.

## Notes for Higher-Level Synthesis

- **Belongs with**: Purchase component — extends Purchase module with enhanced UX for variant selection
- **UX-focused enhancement**: Provides a matrix grid interface (UI pattern) rather than a new business domain
- **Depends on product_matrix infrastructure** — reuses variant matrix components from product_matrix addon
- **Improves purchase order entry efficiency** — focuses on data entry speed for bulk variant purchases
