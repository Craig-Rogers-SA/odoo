# C4 Code Level: Rating Addon

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: addons/rating (root only)
- **Content Hash**: `addons-rating-root-20260701`

## Overview

- **Name**: Rating Addon (Root Package)
- **Description**: Root package module and manifest for Odoo 18 Customer Rating module. Exports models and controllers subpackages.
- **Location**: `addons/rating/` — 2 files (root level only)
- **Language**: Python 3 (Odoo)
- **Purpose**: Enables customer satisfaction feedback (1-5 star ratings, emoji reactions) on helpdesk tickets, project tasks, and live chat interactions. Integrates with mail threads for notification and portal UI.

## Code Elements

### Module Package

- **`rating`** — Root package initialization and manifest for Customer Rating module
  - **Location**: `__init__.py:<1>`, `__manifest__.py:<1>`
  - **Module exports**: `models` (subpackage), `controllers` (subpackage)
  - **Addon metadata**:
    - Name: `Customer Rating`
    - Version: `1.1`
    - Category: `Productivity`
    - License: `LGPL-3`

### Functions / Methods

No top-level functions defined at root level. Implementation split across subdirectories (`models/`, `controllers/`, `tests/`).

### Key Data Files

Configuration and data files defined in manifest (loaded by Odoo framework):
- Models: `rating.rating` (main model), `rating.mixin` (applies rating to documents), `rating.parent_mixin` (applies to parent records), `mail.message` (extended for rating UI), `mail.thread` (extended for rating operations)
- Controllers: HTTP endpoints for rating submission, portal integration, thread chatter widget
- Views: Rating record views, templates (frontend rating UI, emoji selector)
- Security: Model ACLs via `ir.model.access.csv`
- Assets: Backend/frontend JavaScript and CSS for star/emoji rating widgets

## Dependencies

### Internal (Odoo Modules)

- `mail` — Required dependency for mail.thread integration (ratings attached to messages, chatter UI in portal)

### External

- **Web framework assets** (implicit via Odoo):
  - `web.assets_backend` — JavaScript for admin rating management
  - `web.assets_frontend` — SCSS for portal star/emoji display
  - `portal.assets_chatter` — Rating UI in customer portal chatter
  - `mail.assets_public` — Rating widget in public email templates

### Subpackages (Root Directory Imports)

- `models` — ORM models: rating.rating (core), rating.mixin (document tagging), rating_data (default ratings), mail extensions
- `controllers` — HTTP route handlers: public rating submission, portal integration, REST API

## Relationships

No direct relationships defined at root level. Cross-module dependencies:

- **Consumed by**: `helpdesk` (ticket CSAT), `project` (task quality ratings), `im_livechat` (chat quality feedback)
- **Extends**: `mail.message` (rating metadata), `mail.thread` (rating trigger methods), `res.partner` (mixin for contact ratings)

## Notes for Higher-Level Synthesis

- **Domain-bearing core**: Rating is a feedback collection system (1-5 stars or emoji) that mixes with mail and portal infrastructure. Models (`rating.rating`, `rating.mixin`) in subdirectories form domain layer; controllers handle HTTP boundary.
- **Portal integration**: Customers access ratings via portal UI; internal users via chatter widget — dual interface managed in controllers and templates.
- **Mail thread hooks**: Ratings are attached to mail.message objects; trigger notifications via mail templates. Couples tightly with mail module.
- **No admin workflows**: No transient wizards; rating creation is user-initiated via frontend form or API.
- **Mixin pattern**: `rating.mixin` applies rating capability to any model; widely adopted (helpdesk tickets, project tasks, live chat threads).

