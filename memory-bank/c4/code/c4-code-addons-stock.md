# C4 Code Level: addons/stock

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: addons/stock (root files only)
- **Content Hash**: 600894a52a3dd6ba799266f3a3150c0e

## Overview

- **Name**: Odoo 18.0 Stock Module (Root Package)
- **Description**: Root package initialization and hooks for the Inventory/Stock module; orchestrates multi-warehouse inventory management, stock moves, pickings, and lot/serial tracking
- **Location**: addons/stock — 2 files (root), ~50 lines
- **Language**: Python
- **Purpose**: Module entry point that imports controllers, models, reports, and wizards; defines pre-init, post-init, and uninstall hooks for database setup and cleanup

## Code Elements

### Functions / Methods

- `pre_init_hook(env)`
  - **Description**: Clears orphaned stock-related ir.model.data entries before module installation to prevent conflicts
  - **Location**: `__init__.py:11`
  - **Visibility**: internal
  - **Dependencies**: ir.model (Odoo core), stock domain knowledge

- `_assign_default_mail_template_picking_id(env)`
  - **Description**: Assigns the default delivery confirmation email template to companies that lack one during post-init
  - **Location**: `__init__.py:17`
  - **Visibility**: internal
  - **Dependencies**: res.company, mail.template, stock.mail_template_data_delivery_confirmation

- `uninstall_hook(env)`
  - **Description**: Cleans up sequence records associated with stock.picking.type when the module is uninstalled
  - **Location**: `__init__.py:27`
  - **Visibility**: internal
  - **Dependencies**: stock.picking.type, ir.sequence

### Classes / Modules

- **`stock` (package)**
  - **Location**: `__init__.py:1` (implicit)
  - **Sub-packages imported**:
    - `controllers` — HTTP endpoints and portal views
    - `models` — Domain models (stock.move, stock.picking, stock.quant, stock.lot, stock.warehouse, etc.)
    - `report` — Reporting and analytics (stock quantity reports, traceability, reception slips)
    - `wizard` — Transient forms (qty adjustments, replenishment, returns, inventory conflicts)
  - **Dependencies**: product, barcodes_gs1_nomenclature, digest

### Constants / Configuration

- **Manifest data** (from `__manifest__.py`)
  - `name`: 'Inventory'
  - `version`: '1.1'
  - `depends`: ['product', 'barcodes_gs1_nomenclature', 'digest']
  - `category`: 'Inventory/Inventory'
  - `sequence`: 25 (module load order)
  - `installable`: True, `application`: True (end-user-facing module)
  - **Pre/Post/Uninstall hooks**: `pre_init_hook`, `_assign_default_mail_template_picking_id`, `uninstall_hook` (defined in `__init__.py`)
  - **Security**: `security/stock_security.xml`, `security/ir.model.access.csv` (role-based access control for stock models)
  - **Assets**: JavaScript, SCSS, XML templates for web UI, reports, tests

## Dependencies

### Internal

- `controllers` — HTTP handlers and portal integration for stock operations
- `models` — Persistence layer: 27+ model files for stock, warehouse, location, move, picking, quant, lot, package, rule, scrap, barcode, product extensions, company/partner config
- `report` — Business reports: stock quantity, traceability, forecasting, reception slips, lot tracking
- `wizard` — Transient workflows: picking returns, inventory adjustments, replenishment, package allocation, backorder handling, quantity history
- `data/*` — Static data (security, sequences, digests, default barcodes, mail templates)

### External

- `product` (Odoo core) — Product catalog model; stock extends via product.py
- `barcodes_gs1_nomenclature` (Odoo add-on) — GS1 barcode standards and parsing
- `digest` (Odoo core) — Digest email functionality for stock summaries
- `account` (Odoo core) — Used transitively for cost/valuation models

## Relationships

No Mermaid diagram for root package (instantiation point only). See sub-agents' output for models/ and controllers/ structure.

## Notes for Higher-Level Synthesis

- **Core inventory domain**: This module defines the stock domain model. Belongs with all sub-packages (models, controllers, reports, wizards) in a single "Stock / Inventory" component.
- **Boundary code**: controllers/ sub-package contains HTTP entry points for portal (customer POs, delivery tracking).
- **Integration points**: Extends product, company, partner models; integrated with digest for summary emails; depends on account for landed costs and valuation.
- **Hook strategy**: Uses pre/post-init and uninstall hooks to manage sequences and default templates; no complex migrations required at root level.
