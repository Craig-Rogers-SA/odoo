# C4 Code Level: web_editor

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: addons/web_editor
- **Content Hash**: [banyan-c4-20260701-init]

## Overview

- **Name**: Web Editor (Legacy WYSIWYG)
- **Description**: Legacy rich text editor backend for ir.ui.view XML editing with collaborative history tracking and video embed support
- **Location**: addons/web_editor — 23 root-level Python files, ~3800 lines
- **Language**: Python 3 (Odoo ORM)
- **Purpose**: Provides WYSIWYG editing infrastructure for web content and email templates with conflict resolution and embedded field handling

## Code Elements

### Functions / Methods

- `get_video_source_data(video_url: str) → tuple | None`
  - **Description**: Parse video URL and extract platform (youtube, vimeo, dailymotion, instagram) with video ID via regex
  - **Location**: `tools.py:34`
  - **Visibility**: public
  - **Dependencies**: `re` module; uses `valid_url_regex`, `player_regexes` constants

- `get_video_url_data(video_url: str, autoplay: bool = False, loop: bool = False, hide_controls: bool = False, hide_fullscreen: bool = False, hide_dm_logo: bool = False, hide_dm_share: bool = False) → dict`
  - **Description**: Transform video URL into embed-ready form with platform-specific params (YouTube, Vimeo, Dailymotion, Instagram)
  - **Location**: `tools.py:59`
  - **Visibility**: public
  - **Dependencies**: `get_video_source_data`, `urllib.parse.parse_qs`, `werkzeug.urls.url_encode`

- `get_video_embed_code(video_url: str) → Markup | None`
  - **Description**: Generate iframe embed HTML from video URL with query-param preservation (autoplay, loop, controls flags)
  - **Location**: `tools.py:126`
  - **Visibility**: public
  - **Dependencies**: `get_video_url_data`, `markupsafe.Markup`, `urllib.parse`

- `get_video_thumbnail(video_url: str) → bytes | None`
  - **Description**: Fetch platform-specific video thumbnail image (YouTube, Vimeo, Dailymotion, Instagram) via HTTP; returns processed image bytes
  - **Location**: `tools.py:151`
  - **Visibility**: public
  - **Dependencies**: `get_video_source_data`, `requests`, `odoo.tools.image_process`

- `handle_history_divergence(record, html_field_name: str, vals: dict) → None`
  - **Description**: Validate collaborative editing history consistency; broadcast last_step_id via bus.bus; raise ValidationError on divergence
  - **Location**: `tools.py:181`
  - **Visibility**: public
  - **Dependencies**: `re`, `odoo.http.request`, `odoo.exceptions.ValidationError`, bus message channel pattern

- `IrUiView._get_cleaned_non_editing_attributes(attributes: dict) → dict`
  - **Description**: Filter XML attributes for storage, removing branding/editing metadata (data-oe-*, o_editable class, contenteditable)
  - **Location**: `models/ir_ui_view.py:27`
  - **Visibility**: internal
  - **Dependencies**: `EDITING_ATTRIBUTES`, `MOVABLE_BRANDING` constants from base

- `IrUiView.extract_embedded_fields(arch: etree) → list`
  - **Description**: XPath query to locate ORM-backed fields embedded in QWeb templates (data-oe-model != "ir.ui.view")
  - **Location**: `models/ir_ui_view.py:51`
  - **Visibility**: internal (api.model)
  - **Dependencies**: lxml etree XPath

- `IrUiView.extract_oe_structures(arch: etree) → list`
  - **Description**: XPath query to find editable snippets (class oe_structure with id prefix oe_structure)
  - **Location**: `models/ir_ui_view.py:55`
  - **Visibility**: internal (api.model)
  - **Dependencies**: lxml etree XPath

- `IrUiView.save_embedded_field(el: etree.Element) → None`
  - **Description**: Write embedded field value back to ORM via ir.qweb.field converter; handles translation context
  - **Location**: `models/ir_ui_view.py:63`
  - **Visibility**: internal (api.model)
  - **Dependencies**: `ir.qweb.field.*` converters, translation fields

- `apply_patch(initial_content: str, patch: str) → str`
  - **Description**: Apply diff operations (insert +, delete -, replace R) on HTML content using @line,end:text format
  - **Location**: `models/diff_utils.py:35`
  - **Visibility**: public
  - **Dependencies**: `_remove_html_attribute` helper, diff constants

- `Web_Editor.export_icon_to_png(icon: str, color: str = '#000', bg: str = None, size: int = 100, alpha: int = 255, font: str = '...', width: int = None, height: int = None) → Response`
  - **Description**: Convert Font Awesome icon unicode char to PNG image; supports custom colors, background, dimensions, alpha; routed via @http.route
  - **Location**: `controllers/main.py:67`
  - **Visibility**: public
  - **Dependencies**: `PIL.Image`, `PIL.ImageDraw`, `PIL.ImageFont`, werkzeug file serving

- `get_existing_attachment(IrAttachment, vals: dict) → ir.attachment | None`
  - **Description**: Check if attachment with same metadata + checksum already exists; used to deduplicate uploads
  - **Location**: `controllers/main.py:31`
  - **Visibility**: internal
  - **Dependencies**: `IrAttachment._compute_checksum`, base64 codec

### Classes / Modules

- **`IrUiView`** — Odoo model extending ir.ui.view with editor-aware XML manipulation
  - **Location**: `models/ir_ui_view.py:24`
  - **Methods**: `_get_cleaned_non_editing_attributes(...)`, `extract_embedded_fields(...)`, `extract_oe_structures(...)`, `save_embedded_field(...)`, [additional inherited methods]
  - **Implements / Extends**: `odoo.models.Model` (inherits from ir.ui.view)
  - **Dependencies**: `base.models.ir_ui_view.MOVABLE_BRANDING`, lxml, Odoo ORM

- **`Web_Editor`** — HTTP controller for WYSIWYG media operations
  - **Location**: `controllers/main.py:51`
  - **Methods**: `export_icon_to_png(...)`, [additional HTTP handlers if present]
  - **Implements / Extends**: `odoo.http.Controller`
  - **Dependencies**: PIL image processing, werkzeug HTTP, base64 encoding

### Constants / Configuration

- `EDITING_ATTRIBUTES` — List of XML attributes not to persist (data-oe-type, data-oe-expression, etc.)
  - **Location**: `models/ir_ui_view.py:16`
  - **Purpose**: Control which DOM attributes are stripped on save

- `valid_url_regex` — Pattern to validate video URLs before regex matching
  - **Location**: `tools.py:22`
  - **Purpose**: Early filter for malformed URLs before platform detection

- `player_regexes` — Dict of platform-specific video URL patterns (youtube, vimeo, dailymotion, instagram)
  - **Location**: `tools.py:25`
  - **Purpose**: Extract video IDs per platform for embed URL generation

- `diverging_history_regex` — Pattern to extract history step IDs from HTML (data-last-history-steps="1,2,3")
  - **Location**: `tools.py:178`
  - **Purpose**: Detect collaborative edit conflicts via history step tracking

- `PATCH_OPERATION_*` constants — Diff operation type codes (+, -, R for add/remove/replace)
  - **Location**: `models/diff_utils.py:17-22`
  - **Purpose**: Encode patch operations compactly

## Dependencies

### Internal

- `addons.html_editor` — Dependency declared in manifest; web_editor now delegatesOWL-based HTML editing to html_editor (legacy partnership)
- `addons.base.models.ir_ui_view` — Imports MOVABLE_BRANDING for attribute sanitization; extends ir.ui.view
- `odoo.addons.bus` — Uses bus.bus._sendone for collaborative editing conflict notifications via editor_collaboration channel

### External

- `lxml>=4.9` — XML/HTML parsing and manipulation (etree, html modules)
- `PIL (Pillow)>=9.0` — Image processing for icon-to-PNG conversion (Image, ImageDraw, ImageFont)
- `requests>=2.27` — HTTP requests for video thumbnail fetching from platforms
- `werkzeug>=2.2` — WSGI utilities: URL encoding, exceptions, static file serving
- `markupsafe>=2.1` — HTML escaping for iframe embed code generation

## Relationships

```mermaid
classDiagram
    class IrUiView {
        -string id
        -xml arch
        -string xml_id
        +extract_embedded_fields(arch)
        +extract_oe_structures(arch)
        +save_embedded_field(el)
        +_get_cleaned_non_editing_attributes(attrs)
    }
    
    class Web_Editor {
        +export_icon_to_png(icon, color, bg, size)
    }
    
    class DiffUtils {
        +apply_patch(initial_content, patch)
    }
    
    class VideoTools {
        +get_video_source_data(url)
        +get_video_url_data(url, params)
        +get_video_embed_code(url)
        +get_video_thumbnail(url)
        +handle_history_divergence(record, field, vals)
    }
    
    Web_Editor --> IrUiView : saves_arch_to
    VideoTools --> "platform_regex" : uses
    IrUiView --> DiffUtils : applies_patches_via
```

## Notes for Higher-Level Synthesis

- **Component grouping**: Belongs with html_editor component — web_editor is legacy WYSIWYG; html_editor (OWL-based) is the new replacement. Both handle editable views but at different abstraction levels.
- **Boundary code**: HTTP controller (`Web_Editor`) is a system boundary — converts HTTP requests for icon/media operations into ORM writes.
- **Collaboration boundary**: `handle_history_divergence()` bridges editor state (HTML history) and bus messaging — watch for circular references if bus model extends web_editor.
- **Video platform abstraction**: Video tools (tools.py) provide platform-agnostic video URL→embed transformation; low coupling to ORM, reusable outside web_editor.
- **Attr cleaning**: `_get_cleaned_non_editing_attributes()` is a critical filter preventing editor metadata from persisting to database — ensure this is called in all save paths (ir_ui_view extension points).
