# C4 Code Level: odoo/addons/test_exceptions

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: odoo/addons/test_exceptions
- **Content Hash**: 3c8f1b5e9a2d7c4f1a6e9b3d2f8c5a1e

## Overview

- **Name**: test_exceptions
- **Description**: Test module for exception handling and RPC serialization
- **Location**: odoo/addons/test_exceptions — 3 files, ~70 lines
- **Language**: Python
- **Purpose**: Verifies that Odoo exceptions are correctly raised, serialized, and transmitted over RPC to clients

## Code Elements

### Classes / Modules

- **`m`** — Exception test model exposing various Odoo exception types
  - **Location**: `models.py:8`
  - **Type**: Model
  - **Description**: Exposes methods that raise different exception types for RPC client testing
  - **Methods**:
    - `generate_redirect_warning()` → raises RedirectWarning with action reference
    - `generate_access_denied()` → raises AccessDenied exception
    - `generate_access_error(description)` → raises AccessError
    - `generate_exc_access_denied()` → raises native Exception with string 'AccessDenied'
    - `generate_undefined()` → raises AttributeError via undefined symbol access
    - `generate_user_error(description)` → raises UserError
    - `generate_missing_error(description)` → raises MissingError
    - `generate_validation_error(description)` → raises ValidationError
    - `generate_redirect_warning_safe_eval()` → calls generate_redirect_warning via safe_eval
    - `generate_access_denied_safe_eval()` → calls generate_access_denied via safe_eval
    - `generate_access_error_safe_eval()` → calls generate_access_error via safe_eval
    - `generate_exc_access_denied_safe_eval()` → calls generate_exc_access_denied via safe_eval
    - `generate_undefined_safe_eval()` → calls generate_undefined via safe_eval
    - `generate_user_error_safe_eval()` → calls generate_user_error via safe_eval
    - `generate_missing_error_safe_eval()` → calls generate_missing_error via safe_eval
    - `generate_validation_error_safe_eval()` → calls generate_validation_error via safe_eval
    - `generate_safe_eval(f)` → executes function f via safe_eval with globals dict containing 'generate' → f
  - **Exception Types Tested**:
    - RedirectWarning (action-based redirect)
    - AccessDenied (permission denial)
    - AccessError (access violation with description)
    - UserError (user-facing error message)
    - MissingError (record not found)
    - ValidationError (constraint violation)
    - AttributeError (undefined symbol)
    - Native Exception with AccessDenied string
  - **RPC Serialization**: All exceptions tested both directly and via safe_eval to validate serialization in both contexts

## Dependencies

### Internal

- None (isolated exception testing)

### External

- `odoo.models` — Odoo ORM Model base class
- `odoo.api` — Decorators (none used, but module uses @api.model pattern)
- `odoo.exceptions` — Exception types (RedirectWarning, AccessDenied, AccessError, UserError, MissingError, ValidationError)
- `odoo.tools.safe_eval` — Safe expression evaluation with exception propagation
- `odoo.tools.safe_eval.safe_eval` — Safe evaluation function

## Notes for Higher-Level Synthesis

- Exception test suite: validates all Odoo exception types and RPC serialization
- Direct and safe_eval paths: tests exception behavior in both contexts (direct method call and via safe_eval)
- RPC transmission: verifies exceptions serialize correctly for client-side deserialization
- Permission exceptions: AccessDenied and AccessError test security-related errors
- Validation exceptions: ValidationError and UserError test business logic errors
- Redirect handling: RedirectWarning tests action-based redirection
- Pure testing utility: comprehensive exception handling coverage for RPC layer validation
