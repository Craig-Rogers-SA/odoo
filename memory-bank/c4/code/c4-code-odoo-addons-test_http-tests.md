# C4 Code Level: test_http/tests

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: odoo/addons/test_http/tests
- **Content Hash**: e5b9f3c7d2a8b1f6c4e9a3d8f1c6b7a2

## Overview

- **Name**: test_http/tests
- **Description**: HTTP routing and request-handling test suite covering authentication, session, static serving, security, and WebSocket
- **Location**: odoo/addons/test_http/tests — 15 files, ~800+ lines
- **Language**: Python
- **Purpose**: Validates Odoo's HTTP request pipeline: routing, authentication, session management, CORS, static file serving, multipart upload, XSS prevention, WebSocket

## Code Elements

### Base Test Class

- **`TestHttpBase`** — Base class for all HTTP tests with session/geoip mocking
  - **Location**: `test_common.py:<18>`
  - **Base Class**: `HttpCaseWithUserDemo`
  - **Methods**:
    - `setUpClass()` — configures MemorySessionStore, MemoryGeoipResolver, patches odoo.http root
    - `setUp()` — clears session store before each test
    - `db_url_open(url, *args, **kwargs) → Response` — makes authenticated DB request
    - `nodb_url_open(url, *args, **kwargs) → Response` — makes request with empty db_list (patches db_filter)
    - `multidb_url_open(url, *args, dblist=(), **kwargs) → Response` — makes request with multiple databases
    - `parse_http_cache_control(cache_control) → ResponseCacheControl` — parses Cache-Control header
    - `assertCacheControl(response, cache_control)` — validates cache headers
    - `parse_http_expires(expires) → datetime` — parses HTTP-date format
  - **Location**: `test_common.py:<36>` to `<68>`
  - **Visibility**: public
  - **Purpose**: Shared test infrastructure for all HTTP test cases

### Security Tests

- **`TestHttpSecurity`** — Tests HTTP request attribute/environment filtering
  - **Location**: `test_security.py:<5>`
  - **Base Class**: `TestHttpBase`
  - **Methods**:
    - `test_httprequest_attrs()` — validates unsafe attrs stripped (user_agent_class, parameter_storage_class)
    - `test_httprequest_environ()` — validates wsgi/werkzeug internals removed from exposed environ
  - **Location**: `test_security.py:<6>` to `<18>`
  - **Purpose**: Ensures internal HTTP objects not leaked to handlers

### Session Management Tests

- **`TestHttpSession`** — Tests session lifecycle, geoip, multidb authentication
  - **Location**: `test_session.py:<32>`
  - **Base Class**: `TestHttpBase`
  - **Methods**:
    - `test_session0_debug_mode()` — validates debug parameter persists in session
    - `test_session1_default_session()` — validates default session not saved to filestore
    - `test_session3_logout_15_0_geoip()` — validates logout on dbfilter rejection + backward compat for legacy geoip
    - `test_session4_web_authenticate_multidb()` — validates JSON-RPC authentication across databases
  - **Constants**: `GEOIP_ODOO_FARM_2` — mock geoip response
  - **Location**: `test_session.py:<31>` to `<80+>`
  - **Purpose**: Validates session creation, persistence, timezone tracking, multi-database handling

### Static File Tests

- **`TestHttpStaticCommon`** — Base for static file delivery tests
  - **Location**: `test_static.py:<19>`
  - **Base Class**: `TestHttpBase`
  - **Methods**:
    - `assertDownload(url, headers, assert_status_code, assert_headers, assert_content=None)` — validates file download response
    - `assertDownloadGizeh(url, x_sendfile=None, assert_filename='gizeh.png')` — validates PNG image download with optional X-Sendfile header
  - **Fixtures**: `gizeh_data`, `placeholder_data` (preloaded binary files)
  - **Location**: `test_static.py:<31>` to `<50+>`
  - **Purpose**: Shared assertions for static file serving tests (Content-Type, Content-Disposition, caching)

### Misc HTTP Tests

- **`TestHttpMisc`** — Tests reverse proxy, redirects, content negotiation
  - **Location**: `test_misc.py:<23>`
  - **Base Class**: `TestHttpBase`
  - **Methods**:
    - `test_misc0_redirect()` — validates double-slash redirect handling (werkzeug version-dependent)
    - `test_misc1_reverse_proxy()` — validates X-Forwarded-* header handling (proxy_mode: false doesn't trust)
  - **Location**: `test_misc.py:<24>` to `<50+>`
  - **Purpose**: Tests edge cases in HTTP request handling

### Test File Summary

| Test File | Focus | Base Class |
|-----------|-------|-----------|
| `test_common.py` | Shared test infrastructure | HttpCaseWithUserDemo |
| `test_security.py` | Request filtering | TestHttpBase |
| `test_session.py` | Session lifecycle | TestHttpBase |
| `test_static.py` | Static file serving | TestHttpBase |
| `test_misc.py` | Reverse proxy, redirects | TestHttpBase |
| `test_device.py` | Device detection | TestHttpBase |
| `test_echo_reply.py` | Echo handler responses | TestHttpBase |
| `test_error.py` | Error handling | TestHttpBase |
| `test_greeting.py` | Basic routing | TestHttpBase |
| `test_models.py` | Model access via HTTP | TestHttpBase |
| `test_registry.py` | Module registry | TestHttpBase |
| `test_webjson.py` | JSON-RPC protocol | TestHttpBase |
| `test_web_server.py` | Server startup/shutdown | TestHttpBase |
| `test_xss.py` | XSS prevention | TestHttpBase |

## Dependencies

### Internal

- `odoo.addons.test_http.utils` — MemoryGeoipResolver, MemorySessionStore, TEST_IP constant
- `odoo.addons.test_http.controllers` — CT_JSON constant

### External

- `odoo.http` — Session, root, content_disposition, Application
- `odoo.tests.common` — HttpCaseWithUserDemo, new_test_user, get_db_name, BaseCase, HOST, RecordCapturer
- `odoo.tools` — config, file_open, file_path, parse_version, lazy_property, image_process, mute_logger
- `werkzeug` — datastructures, http, HTTP utilities
- `freezegun` — time mocking for temporal tests
- `unittest.mock` — patch decorator for mocking
- `urllib` — URL parsing and encoding
- `pytz` — timezone handling
- `json` — JSON-RPC request/response
- `datetime` — timestamp handling
- `socket` — gethostbyname

## Notes for Higher-Level Synthesis

- HTTP framework test suite — validates request/response pipeline, session handling, static file serving
- Boundary: Tests Werkzeug integration, WSGI environment filtering, routing resolution
- Tight coupling to test_http module controllers (expects specific routes and response formats)
- Tests timezone-aware session tracking, multi-database selection, reverse proxy headers, XSS mitigation
- Includes device/browser detection tests (User-Agent parsing) and echo/error handling
