# C4 Code Level: test_lint

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: odoo/addons/test_lint
- **Content Hash**: f2e7a1c9d4b6c3f8a5e2b7d1c9f6a4e8

## Overview

- **Name**: test_lint
- **Description**: Automated code quality tests for pylint rules, manifest validation, XML/CSV integrity, i18n checks
- **Location**: odoo/addons/test_lint — 21 files, ~1200+ lines
- **Language**: Python
- **Purpose**: Validates Odoo's custom code quality rules: SQL injection detection, gettext usage, pylint compliance, manifest structure, i18n file format

## Code Elements

### Base Classes / Utilities

- **`LintCase`** — Base class for file-walking lint test cases
  - **Location**: `tests/lint_case.py:<10>`
  - **Base Class**: `odoo.tests.BaseCase`
  - **Method**: `iter_module_files(*globs, modules=None) → Iterator[str]`
    - Yields file paths matching glob patterns across modules
    - **Location**: `tests/lint_case.py:<14>`
    - **Visibility**: public

- **`NodeVisitor`** — AST visitor utility for code analysis
  - **Location**: `tests/lint_case.py:<26>`
  - **Methods**:
    - `visit(node) → any` — dispatches to visit_<NodeType> method
    - `generic_visit(node) → Generator` — yields child node visits
  - **Purpose**: Generic AST traversal pattern for custom code checkers

### Test Classes

- **`TestPylintChecks`** — Base class for pylint checker tests
  - **Location**: `tests/test_checkers.py:<46>`
  - **Base Class**: `odoo.tests.common.TransactionCase`
  - **Method**: `check(test_content, plugins, rules) → Tuple[int, List[dict]]`
    - Executes pylint with custom plugins; returns exit code + JSON-parsed violations
    - **Location**: `tests/test_checkers.py:<47>`
    - Spawns subprocess: pylint --load-plugins --enable <rules> --output-format=json
    - **Visibility**: public

- **`TestGetTextLint`** — Tests gettext() call validation (pylint plugin)
  - **Location**: `tests/test_checkers.py:<74>`
  - **Base Class**: `TestPylintChecks`
  - **Methods**:
    - `test_gettext_env()` — validates _() and self.env._() receive single placeholder
  - **Rule**: gettext-placeholders (rejects *args in _() calls)

- **`TestSqlLint`** — Tests SQL injection detection (pylint plugin)
  - **Location**: `tests/test_checkers.py:<96>`
  - **Base Class**: `TestPylintChecks`
  - **Rule**: sql-injection (detects string concatenation in SQL queries)

- **`ManifestLinter`** — Tests __manifest__.py structure
  - **Location**: `tests/test_manifests.py:<23>`
  - **Base Class**: `odoo.tests.BaseCase`
  - **Methods**:
    - `_load_manifest(module) → dict` — parses __manifest__.py without defaults
    - `test_manifests()` — iterates all modules and validates keys/values
    - `_test_manifest_keys(module, manifest_data)` — checks for unknown keys
  - **Manifest Keys Whitelist**: name, icon, addons_path, license, contributors, maintainer, url, price, currency, support, live_test_url
  - **Purpose**: Enforces consistent manifest structure across codebase

- **`TestPyLint`** — Full PyLint suite against all modules
  - **Location**: `tests/test_pylint.py:<25>`
  - **Base Class**: `odoo.tests.TransactionCase`
  - **Method**: `test_pylint()`
    - Validates pylint version >= 1.7.0
    - Runs pylint against all non-addon modules with enabled checks
  - **Purpose**: Enforces Python code quality across framework

- **`TestDunderinit`** — Validates __init__.py existence in modules
  - **Location**: `tests/test_dunderinit.py:<15>`
  - **Base Class**: `lint_case.LintCase`
  - **Method**: `test_dunderinit()` — checks all modules have __init__.py (whitelist: test_data_module)

- **`UnittestLinter`** — Mock PyLinter for unit testing custom checkers
  - **Location**: `tests/test_checkers.py:<27>`
  - **Base Class**: `pylint.lint.PyLinter`
  - **Methods**:
    - `add_message(msg_id, *args, **kwargs)` — appends message ID to _messages list
    - `is_message_enabled(*_args, **kwargs)` → bool (always True)
  - **Purpose**: Enables testing custom pylint plugins in isolation

### Custom Pylint Checker Modules

- **`_odoo_checker_sql_injection`** — Detects SQL injection patterns (`tests/_odoo_checker_sql_injection.py`)
- **`_odoo_checker_gettext`** — Validates gettext() placeholder usage (`tests/_odoo_checker_gettext.py`)
- **`_odoo_checker_unlink_override`** — Validates unlink() method signatures (`tests/_odoo_checker_unlink_override.py`)

## Dependencies

### Internal

- None — isolated test module for framework code quality

### External

- `pylint` — Python static analysis engine
- `astroid` — AST library used by pylint
- `odoo.tests` — BaseCase, TransactionCase, testing utilities
- `odoo.modules` — get_modules(), get_module_path()
- `odoo.tools` — misc utilities, file_open, config
- `subprocess` — spawn pylint binary
- `tempfile` — temporary file creation for test code
- `fnmatch` — glob pattern matching
- `ast` — Python AST module
- `json` — JSON parsing of pylint output
- `logging` — logger setup
- `pathlib` — file path operations

## Notes for Higher-Level Synthesis

- Framework quality assurance component — validates codebase against custom Odoo rules
- Boundary: Tests framework conventions (manifest keys, security patterns, i18n rules)
- Pure test infrastructure — not production code; used in CI to enforce quality standards
- Tight coupling to custom pylint plugins (framework-specific checkers for SQL injection, gettext, unlink overrides)
