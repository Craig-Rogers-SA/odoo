# C4 Code Level: odoo/conf

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: odoo/conf
- **Content Hash**: 7c9a5f2e8d1b4a6f3c2e9a1b5d8f4e7c

## Overview

- **Name**: Odoo Configuration Module
- **Description**: Library-wide configuration variables and initialization settings for Odoo.
- **Location**: odoo/conf — 1 file, 26 lines
- **Language**: Python
- **Purpose**: Centralizes configuration state (addon paths, server-wide modules) initialized by `openerp.tools.config.parse_config()`. Acts as a module-level registry for configuration loaded from odoo.conf files and command-line arguments.

## Code Elements

### Module Variables (Configuration State)

- **`addons_paths`** — List of filesystem paths to search for OpenERP addons; initialized as empty list, populated by config parser
  - **Location**: `odoo/conf/__init__.py:19`
  - **Type**: `list[str]`
  - **Visibility**: Public (module-level)
  - **Usage**: Read by addon loader; written by `openerp.tools.config.parse_config()`

- **`server_wide_modules`** — List of module names to load server-wide (not bound to specific databases); initialized as empty list, set via `--load` command-line option
  - **Location**: `odoo/conf/__init__.py:26`
  - **Type**: `list[str]`
  - **Visibility**: Public (module-level)
  - **Usage**: Read during server startup; written by config parser from CLI flags

## Dependencies

### Internal

- `openerp.tools.config` — Provides `parse_config()` function that populates this module's state; handles odoo.conf file parsing and command-line argument processing

### External

- None (standard library only)

## Notes for Higher-Level Synthesis

- **Scope**: Pure configuration state holder — no business logic, no domain rules. Acts as a module-level singleton registry.
- **Boundary**: This module is a **boundary artifact** between the config-parsing layer (`openerp.tools.config`) and the rest of Odoo's initialization chain. It decouples parsing logic from state storage.
- **Integration point**: Central place where configuration from multiple sources (odoo.conf file, CLI flags, environment) converges into canonical Python variables for use throughout the system.

