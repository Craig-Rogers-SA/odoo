# C4 Code Level: odoo/tools/_vendor

<!--
  Auto-generated by the c4-code agent. One file per source directory.
  Refreshed on /banyan-c4 reruns whenever the directory's content hash changes.
  Do NOT hand-edit; edits will be overwritten on next refresh.
-->

## Generated By

- **Agent**: c4-code (Haiku)
- **Run ID**: banyan-c4-20260701-init
- **Generated**: 2026-07-01T00:00:00Z
- **Source Directory**: odoo/tools/_vendor
- **Content Hash**: [vendored-stdlib-werkzeug-utilities]

## Overview

- **Name**: Vendored Werkzeug Utilities
- **Description**: Backport and vendored copies of Werkzeug utilities for OS compatibility and deprecated functionality
- **Location**: odoo/tools/_vendor — 4 files, ~450 lines
- **Language**: Python
- **Purpose**: Provides HTTP file streaming, session management, and user-agent parsing for older OS distributions that have not yet upgraded to newer Werkzeug versions

## Code Elements

### Functions / Methods

- `send_file(path_or_file: Union[str, PathLike, IO[bytes]], environ: WSGIEnvironment, mimetype: Optional[str] = None, as_attachment: bool = False, download_name: Optional[str] = None, conditional: bool = True, etag: Union[bool, str] = True, last_modified: Optional[Union[datetime, int, float]] = None, max_age: Optional[Union[int, Callable]] = None, use_x_sendfile: bool = False, response_class: Optional[Type[Response]] = None, _root_path: Optional[Union[str, PathLike]] = None) → Response`
  - **Description**: Send file contents to WSGI client with support for conditional requests, ETags, range responses, and X-Sendfile optimization
  - **Location**: `send_file.py:32`
  - **Visibility**: public
  - **Dependencies**: werkzeug (Response, Headers, wrap_file, url_quote, RequestedRangeNotSatisfiable), stdlib (io, mimetypes, os, datetime, zlib)

- `generate_key(salt: Optional[bytes] = None) → str`
  - **Description**: Generate a cryptographically unique session key via SHA-1 hash of salt, current time, and random bytes
  - **Location**: `sessions.py:33`
  - **Visibility**: public
  - **Dependencies**: hashlib, os, time

- `UserAgent(environ_or_string: Union[dict, str])`
  - **Description**: Parse and inspect user-agent strings to extract platform, browser, version, and language information
  - **Location**: `useragents.py:184`
  - **Visibility**: public (constructor)
  - **Dependencies**: re

- `UserAgent.to_header() → str`
  - **Description**: Return the raw user-agent string as HTTP header value
  - **Location**: `useragents.py:192`
  - **Visibility**: public
  - **Dependencies**: none (identity return)

### Classes / Modules

- **`ModificationTrackingDict`** — Dict subclass that tracks when contents are modified via callback
  - **Location**: `sessions.py:39`
  - **Methods**: `__init__(...)`, `copy()`, `__copy__()`
  - **Implements / Extends**: CallbackDict (werkzeug.datastructures)
  - **Dependencies**: werkzeug.datastructures.CallbackDict

- **`Session`** — Mutable session object with identity, versioning, and save-tracking
  - **Location**: `sessions.py:64`
  - **Methods**: `__init__(data, sid, new=False)`, `__repr__()`, `should_save` (property)
  - **Implements / Extends**: ModificationTrackingDict
  - **Dependencies**: internal (ModificationTrackingDict)

- **`SessionStore`** — Base class for all session persistence backends
  - **Location**: `sessions.py:95`
  - **Methods**: `__init__(session_class=None)`, `is_valid_key(key: str) → bool`, `generate_key(salt=None) → str`, `new() → Session`, `save(session)`, `save_if_modified(session)`, `delete(session)`, `get(sid: str) → Session`
  - **Implements / Extends**: object
  - **Dependencies**: re (SHA-1 validation)

- **`FilesystemSessionStore`** — File-backed session store using JSON serialization
  - **Location**: `sessions.py:144`
  - **Methods**: `__init__(path=None, filename_template='werkzeug_%s.sess', session_class=None, renew_missing=False, mode=0o644)`, `get_session_filename(sid) → str`, `save(session)`, `delete(session)`, `get(sid) → Session`, `list() → list[str]`
  - **Implements / Extends**: SessionStore
  - **Dependencies**: os, tempfile, json, stdlib (file I/O)

- **`UserAgentParser`** — Stateless regex-based parser for browser and platform detection
  - **Location**: `useragents.py:19`
  - **Methods**: `__init__()` (compiles regex tables), `__call__(user_agent: str) → tuple[str, str, str, str]` (returns platform, browser, version, language)
  - **Implements / Extends**: object
  - **Dependencies**: re

- **`UserAgent`** — Wrapper for user-agent parsing with convenience properties
  - **Location**: `useragents.py:106`
  - **Methods**: `__init__(environ_or_string)`, `to_header() → str`, `__str__()`, `__bool__()`, `__repr__()`
  - **Implements / Extends**: object
  - **Properties**: `string`, `platform`, `browser`, `version`, `language`
  - **Dependencies**: internal (UserAgentParser)

### Constants / Configuration

- `SERIALIZABLE_TYPES` — tuple of allowed session data types (`type(None), bool, int, float, str, bytes, tuple, list, dict`) — `sessions.py:8`
- `_fs_transaction_suffix` — suffix for temporary files during atomic session writes (`.__wz_sess`) — `sessions.py:141`

## Dependencies

### Internal

- None — this is a utility vendoring layer; not imported by other Odoo directories in read-only mode

### External

- `werkzeug >= 1.0` — HTTP utilities (Response, wrap_file, Headers, url_quote, datastructures)
- `stdlib (io, mimetypes, os, re, json, tempfile, hashlib, zlib, datetime, time, unicodedata)` — standard library

## Relationships

No diagram needed — these are straightforward utility functions and stateless classes. Relationships are unidirectional imports from Werkzeug only.

## Notes for Higher-Level Synthesis

- **Pure vendoring layer** — Odoo tools module uses these to maintain compatibility with Debian 12 / Ubuntu 22.04 which package older Werkzeug versions. These are not domain-bearing and should not be grouped into business components.
- **HTTP boundary code** — `send_file()` is the only code element that handles WSGI environ directly; it's called by HTTP response handlers in the web/ directory.
- **Session state** — `FilesystemSessionStore` is instantiated once during app initialization but represents a technical concern (persistence), not a business domain.
